<UserControl x:Class="SchoolManagement.Views.Student.StudentDetailsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SchoolManagement.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Content="Button" HorizontalAlignment="Left" Margin="680,403,0,0" VerticalAlignment="Top"/>
        <TextBox HorizontalAlignment="Left" Margin="65,78,0,0"
                 Background="Transparent" 
                 Foreground="White" 
                 TextWrapping="Wrap"
                 Text="Name"
                 VerticalAlignment="Top"
                 Width="120"/>
        <TextBox HorizontalAlignment="Left" Margin="65,122,0,0"
                 Background="Transparent" 
                 Foreground="White" 
                 TextWrapping="Wrap"
                 Text="Name"
                 VerticalAlignment="Top"
                 Width="120"/>

    </Grid>
</UserControl>
